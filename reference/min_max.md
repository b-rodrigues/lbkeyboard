# Standardizes an atomic vector of numbers using the min-max method

Standardizes an atomic vector of numbers using the min-max method

## Usage

``` r
min_max(x)
```

## Arguments

- x:

  An atomic vector of numbers

## Examples

``` r
if (FALSE) { # \dontrun{
min_max(seq(1, 10))
} # }
```
