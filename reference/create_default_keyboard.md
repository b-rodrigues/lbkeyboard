# Create a default keyboard layout for optimization

Creates a standard ISO keyboard layout data frame with 30 letter key
positions.

## Usage

``` r
create_default_keyboard()
```

## Value

A data frame with columns: key, row, number, x_mid, y_mid
