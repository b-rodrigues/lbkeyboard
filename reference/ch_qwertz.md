# Swiss French keyboard

A dataset containing rows and keys of a full ISO Swiss French layout, as
commonly used in Luxembourg

## Usage

``` r
ch_qwertz
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 119
rows and 19 columns.
