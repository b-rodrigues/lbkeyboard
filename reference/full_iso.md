# Full Keyboard

A dataset containing rows and keys in a full mechanical keyboard, ISO
layout.

## Usage

``` r
full_iso
```

## Format

An object of class `spec_tbl_df` (inherits from `tbl_df`, `tbl`,
`data.frame`) with 119 rows and 8 columns.
