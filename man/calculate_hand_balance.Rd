% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_hand_balance.R
\name{calculate_hand_balance}
\alias{calculate_hand_balance}
\title{Calculate hand balance for a keyboard layout}
\usage{
calculate_hand_balance(kb, freq_df, threshold = NULL)
}
\arguments{
\item{kb}{A keyboard layout data frame (must contain \code{key} and \code{number} columns).}

\item{freq_df}{A data frame with \code{characters} and \code{frequencies} columns.}

\item{threshold}{Integer. The column index where the split occurs. Keys with
\code{number < threshold} are assigned to the left hand. If NULL (default),
the threshold is adaptively determined based on the minimum column index
(5 for 0-based indexing, 6 for 1-based indexing).}
}
\value{
A string formatted as "Left\%/Right\%".
}
\description{
Calculates the percentage of keystrokes performed by the left and right hands
based on a keyboard layout and letter frequencies.
}
\examples{
data("ch_qwertz")
data("english")
freq <- letter_freq(english)
calculate_hand_balance(ch_qwertz, freq)
}
