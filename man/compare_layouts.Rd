% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_layout.R
\name{compare_layouts}
\alias{compare_layouts}
\title{Compare effort across multiple keyboard layouts}
\usage{
compare_layouts(
  keyboards,
  text_samples,
  keys_to_evaluate = letters,
  effort_weights = list(base = 1, same_finger = 3, same_hand = 1, row_change = 0.5)
)
}
\arguments{
\item{keyboards}{Named list of keyboard data frames to compare.}

\item{text_samples}{Character vector of text samples.}

\item{keys_to_evaluate}{Character vector of keys to include. Default is lowercase letters.}

\item{effort_weights}{Named list of effort weights.}
}
\value{
A data frame with layout names and their effort scores, sorted by effort.
}
\description{
Calculate and compare typing effort for multiple keyboard layouts.
}
\examples{
\dontrun{
data(afnor_bepo)
data(afnor_azerty)
data(ch_qwertz)
data(french)

comparison <- compare_layouts(
  keyboards = list(
    BEPO = afnor_bepo,
    AZERTY = afnor_azerty,
    QWERTZ = ch_qwertz
  ),
  text_samples = french
)
print(comparison)
}
}
