% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_layout.R
\name{calculate_layout_effort}
\alias{calculate_layout_effort}
\title{Calculate typing effort for a keyboard layout}
\usage{
calculate_layout_effort(
  keyboard,
  text_samples,
  keys_to_evaluate = letters,
  effort_weights = list(base = 3, same_finger = 3, same_hand = 0.5, row_change = 0.5),
  breakdown = FALSE
)
}
\arguments{
\item{keyboard}{A keyboard data frame with columns \code{key}, \code{row}, \code{number}.}

\item{text_samples}{Character vector of text samples to evaluate.}

\item{keys_to_evaluate}{Character vector of keys to include. Default is lowercase letters.}

\item{effort_weights}{Named list of effort weights (see \code{\link{optimize_layout}}).}

\item{breakdown}{Logical. Return detailed breakdown of effort components? Default FALSE.}
}
\value{
If \code{breakdown = FALSE}, a single numeric value (total effort).
If \code{breakdown = TRUE}, a list with effort components:
\describe{
\item{total_effort}{Total weighted effort}
\item{base_effort}{Effort from individual key presses}
\item{same_finger_effort}{Effort from same-finger bigrams}
\item{same_hand_effort}{Effort from same-hand sequences}
\item{row_change_effort}{Effort from row changes}
\item{same_finger_bigrams}{Count of same-finger bigrams}
\item{same_hand_bigrams}{Count of same-hand bigrams}
\item{hand_alternations}{Count of hand alternations}
}
}
\description{
Computes the total typing effort for a given keyboard layout and text samples
using a Carpalx-inspired effort model.
}
\examples{
\dontrun{
data(afnor_bepo)
data(french)

# Calculate effort for BEPO layout
effort <- calculate_layout_effort(afnor_bepo, french)
print(effort)

# Get detailed breakdown
breakdown <- calculate_layout_effort(afnor_bepo, french, breakdown = TRUE)
print(breakdown)
}
}
